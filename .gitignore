.vscode
.claude
target
data/raw
data/

# Map files
*.nc

# MCAP test output files
*.mcap

# Ignore latex junk
*.aux
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.bbl
*.blg
*.bcf
*.zip
*.nav
*.snm
*.toc
*.vrb
*.run.xml

# Leftover pixi environments. pixi.toml itself was removed in #335 -- the repository is
# plain cargo now -- but an existing .pixi/ directory should stay out of the index.
.pixi

# Data output
data/output/*

papers/anomaly_ukf/data/combined_statistics_error.csv

# mdBook output
book/book/

# Generated performance plots
data/output/*.png

# Research artifacts kept locally, out of the v1.0 release scope.
# See papers/README.md for where these live now.
analysis/
papers/anomaly_rbpf/
papers/anomaly_ukf/
papers/data_paper/
data.ipynb
analysis_rbpf_results.py
README_files/
log/

# Analysis notebooks, untracked in #335 for the same reason as analysis/: they import pygmt,
# cartopy, numpy and filterpy against the Python environment that pixi used to provide, and
# nothing in this Rust-only repository can run them. Kept on disk, out of the index. The map
# provenance they used to be the only record of now lives in geonav/README.md.
examples/geonav-sim/
examples/strapdown-sim/
